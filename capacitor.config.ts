import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'oil_station-service',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
